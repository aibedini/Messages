<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_SMS"/>
    <uses-permission android:name="android.permission.RECEIVE_SMS"/>
    <uses-permission android:name="android.permission.SEND_SMS"/>
    <uses-permission android:name="android.permission.WRITE_SMS"/>
    <!-- Required only to list SIM slots/subscriptions in Settings > Messaging -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_MMS"/>
    <uses-permission android:name="android.permission.RECEIVE_WAP_PUSH"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <!-- v2.6.11: the GMweb pull bridge must survive Doze. A partial wake lock
         is held ONLY for the duration of each long-poll HTTP round trip (the
         radio + CPU must stay alive while the phone is "waiting for a task");
         between cycles the device sleeps normally. Without WAKE_LOCK the
         long-poll silently dies in Doze and GMweb answers
         503 android_gateway_unreachable to every Eve send. -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>

    <application
        android:name=".MessagesApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Messages">

        <!-- Main launcher activity + compose intent filters (required for default SMS app) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Messages"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- sms:/smsto: links (browser, contacts, dialer). Scheme-only
                 filter: must NOT carry a mimeType or SEND stops matching. -->
            <intent-filter>
                <action android:name="android.intent.action.SENDTO" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
            <!-- Share sheet / "Open with" for plain text. Separate filter per
                 the Android docs on receiving simple data. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!--
            SMS_DELIVER fires only when this is the default SMS app.
            SMS_RECEIVED fires for all apps (non-default listening).
        -->
        <receiver
            android:name=".receiver.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_DELIVER" />
            </intent-filter>
        </receiver>

        <!-- WAP/MMS push: mmslib PushReceiver persists the notification-indication
             and downloads the payload from the carrier MMSC, then broadcasts
             MMS_RECEIVED to our .receiver.MmsReceiver below. Required to be
             listed as default SMS app. -->
        <receiver
            android:name="com.android.mms.transaction.PushReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_WAP_PUSH">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.WAP_PUSH_DELIVER" />
                <data android:mimeType="application/vnd.wap.mms-message" />
            </intent-filter>
        </receiver>

        <!-- Receives the library's explicit MMS_RECEIVED broadcast after the
             download completes; taskAffinity is how mmslib's BroadcastUtils
             finds this receiver class. The broadcast is explicit AND
             package-scoped by the library, so no external export needed. -->
        <receiver
            android:name=".receiver.MmsReceiver"
            android:exported="false"
            android:taskAffinity="com.klinker.android.messaging.MMS_RECEIVED" />

        <!-- Drives MMS download/send transactions over the MMS network. -->
        <service android:name="com.android.mms.transaction.TransactionService" />

        <!-- Notification action receiver -->
        <receiver
            android:name=".receiver.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.autonomousone.messages.ACTION_MARK_READ" />
                <action android:name="com.autonomousone.messages.ACTION_REPLY" />
                <action android:name="com.autonomousone.messages.ACTION_COPY_OTP" />
            </intent-filter>
        </receiver>

        <!-- Gateway reboot recovery: replays ACTION_START when the user left
             the gateway enabled before the reboot (gatewayDesiredEnabled). -->
        <receiver
            android:name=".receiver.BootGatewayReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Explicit SmsManager SENT/DELIVERED callbacks. Manifest lifetime is
             required so status updates survive closing the conversation/app. -->
        <receiver
            android:name=".sms.SmsStatusReceiver"
            android:exported="false" />

        <!-- Respond-via-message service required for default SMS app -->
        <service
            android:name=".service.HeadlessSmsSendService"
            android:exported="true"
            android:permission="android.permission.SEND_RESPOND_VIA_MESSAGE">
            <intent-filter>
                <action android:name="android.intent.action.RESPOND_VIA_MESSAGE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="sms" />
                <data android:scheme="smsto" />
                <data android:scheme="mms" />
                <data android:scheme="mmsto" />
            </intent-filter>
        </service>
        <!-- FileProvider for sharing camera capture URIs -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Gateway Foreground Service -->
        <service
            android:name=".gateway.GatewayService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync|specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="SMS Gateway Local REST API Server" />
        </service>

    </application>

</manifest>
