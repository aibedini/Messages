<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Must be an AppCompat theme: MainActivity extends AppCompatActivity
        (required by androidx.biometric). Platform Material themes crash it
        at startup with "You need to use a Theme.AppCompat theme".
    -->
    <style name="Theme.Messages" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
